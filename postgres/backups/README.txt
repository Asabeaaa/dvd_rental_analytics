Put PostgreSQL backup files here (.tar, .backup, .sql, .dump).

For this lab:
- dvdrental.tar — restore through pgAdmin (see main README)
- Any custom dumps you create in class

Files in this folder are visible inside the postgres container at /backups
and inside pgAdmin when browsing server backups.
